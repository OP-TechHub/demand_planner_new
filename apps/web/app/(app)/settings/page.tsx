import { Placeholder } from '@/components/placeholder';

export default function SettingsPage() {
  return <Placeholder title="Plan Settings" note="Margin metric, allocation mode, scope, lookback — later slice of Session 2." />;
}
